# 字音预生成 POC 横评 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `poc/audio-tts/` 下完成 edge-tts / GPT-SoVITS / CoquiTTS 三家在 20 个汉字上的盲听打分对比，产出 CSV 数据用于"是否进入正式字音预生成方案"的决策。

**Architecture:** POC 完全隔离：所有代码与产物位于 `poc/audio-tts/`，不修改 `server/` / `client/` 任何运行时代码、不污染主项目 `package.json`。Node 脚本（charset 抽取、edge baseline、对比页生成）通过 `npx tsx` 直接运行，沿用主项目 root 已有的 `tsx`；Python 脚本（sovits / coqui）各自独立 venv。三家输出统一为 mp3 24kHz 单声道 48kbps，文件名格式 `<两位序号>-<字>.mp3`。打分页是纯静态 HTML，把 mp3 文件名和盲听 seed 直接 inline 进 JS。

**Tech Stack:** Node + TypeScript（tsx 直跑）、msedge-tts、Python 3.10+、GPT-SoVITS、Coqui TTS、ffmpeg、纯静态 HTML/JS（无框架）。

参考 spec: `docs/superpowers/specs/2026-04-29-tts-poc-design.md`

---

## 文件结构

```
poc/audio-tts/
├── README.md                    # 操作手册 + 结论判读规则
├── charset.json                 # 20 字（提交到仓库，可复现）
├── .gitignore                   # 忽略 audio/ 与 results/*.csv
├── scripts/
│   ├── pick-charset.ts          # Task 2
│   ├── gen-edge.ts              # Task 3
│   ├── gen-sovits.py            # Task 4
│   ├── gen-coqui.py             # Task 5
│   ├── requirements-sovits.txt  # Task 4
│   ├── requirements-coqui.txt   # Task 5
│   └── build-compare-page.ts    # Task 6
├── audio/                       # gitignore，运行时生成
│   ├── edge/01-妈.mp3 ...
│   ├── sovits/01-妈.mp3 ...
│   └── coqui/01-妈.mp3 ...
├── compare.html                 # Task 6 输出
└── results/                     # 用户手动放入打分 csv
    └── .gitkeep
```

每个文件的职责：
- `charset.json`：字表 + seed + 时间戳，三家脚本的唯一入口。
- `pick-charset.ts`：seed=42，从内置常用字池随机抽 20 字，写 `charset.json`。
- `gen-edge.ts`：读 `charset.json`，调 `msedge-tts` 生成 mp3 到 `audio/edge/`。
- `gen-sovits.py` / `gen-coqui.py`：Python 各自独立 venv，输出 wav 后 `ffmpeg` 转 mp3。
- `build-compare-page.ts`：读 `charset.json`，生成 `compare.html`（盲听打分 + CSV 导出）。
- `README.md`：跑步骤、环境要求、判读规则。

---

## Task 1: POC 目录骨架与 .gitignore

**Files:**
- Create: `poc/audio-tts/.gitignore`
- Create: `poc/audio-tts/README.md`
- Create: `poc/audio-tts/results/.gitkeep`

- [ ] **Step 1: 创建目录骨架**

```bash
mkdir -p poc/audio-tts/scripts poc/audio-tts/audio poc/audio-tts/results
touch poc/audio-tts/results/.gitkeep
```

- [ ] **Step 2: 写 .gitignore**

文件 `poc/audio-tts/.gitignore`：

```gitignore
# 生成的音频可重跑，不入库
audio/
# 打分结果默认不入库，需要时手动 git add
results/*.csv
# Python venv
.venv-*/
__pycache__/
# 生成的对比页
compare.html
```

- [ ] **Step 3: 写 README 占位**

文件 `poc/audio-tts/README.md`：

````markdown
# 字音预生成 POC：开源 TTS 横评

对应 spec：`docs/superpowers/specs/2026-04-29-tts-poc-design.md`

## 跑通流程

```bash
# 1. 抽 20 字 → charset.json
npx tsx poc/audio-tts/scripts/pick-charset.ts

# 2. 生成三家 mp3
npx tsx poc/audio-tts/scripts/gen-edge.ts

python3 -m venv poc/audio-tts/.venv-sovits
source poc/audio-tts/.venv-sovits/bin/activate
pip install -r poc/audio-tts/scripts/requirements-sovits.txt
python poc/audio-tts/scripts/gen-sovits.py
deactivate

python3 -m venv poc/audio-tts/.venv-coqui
source poc/audio-tts/.venv-coqui/bin/activate
pip install -r poc/audio-tts/scripts/requirements-coqui.txt
python poc/audio-tts/scripts/gen-coqui.py
deactivate

# 3. 生成打分页并打开
npx tsx poc/audio-tts/scripts/build-compare-page.ts
open poc/audio-tts/compare.html
```

## 系统依赖

- Node 20+（主项目 root 的 `tsx` 已可用）
- Python 3.10+
- ffmpeg（`brew install ffmpeg`）

## 任一引擎装不上

可只跑剩下两家。打分页只显示成功生成的列。结论判读规则同样适用，结论维度变少。

## 结论判读规则

| 情况 | 行动 |
|---|---|
| sovits 或 coqui 平均分 ≥ 4.0 且比 edge 高 ≥ 0.5 | 进入正式预生成方案（开新 design） |
| sovits/coqui 都 < edge | POC 否决；转向云童声 TTS |
| 三家平均分差 < 0.3 | 维持 edge baseline |
````

- [ ] **Step 4: 提交**

```bash
git add poc/audio-tts/.gitignore poc/audio-tts/README.md poc/audio-tts/results/.gitkeep
git commit -m "chore(poc): 字音 TTS 横评 POC 骨架"
```

---

## Task 2: 抽 20 字脚本 `pick-charset.ts`

**Files:**
- Create: `poc/audio-tts/scripts/pick-charset.ts`
- Create: `poc/audio-tts/charset.json`（脚本运行后产出）

**说明：** 内置一个 ~120 字的常用字池（涵盖单韵母、多音字、轻声、卷舌音等代表字），用 mulberry32 PRNG（seed=42）抽 20 个。这样字池+seed 都进仓库，结果完全可复现。

- [ ] **Step 1: 写抽取脚本**

文件 `poc/audio-tts/scripts/pick-charset.ts`：

```typescript
import { writeFileSync, mkdirSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const SEED = 42;

// 常用字池：刻意覆盖单韵母、多音字、轻声字、卷舌音、儿化基础字
const POOL = [
  '妈','马','骂','麻','爸','白','百','北','本','不',
  '波','播','破','婆','泼','啊','爱','安','按','岸',
  '欧','偶','藕','怕','拍','排','派','跑','炮','陪',
  '是','时','十','使','市','师','石','史','水','睡',
  '吃','车','尺','齿','池','茶','长','常','场','唱',
  '日','人','认','热','软','弱','若','然','让','绕',
  '知','只','纸','直','志','中','种','重','住','主',
  '行','银','应','英','影','音','因','一','以','已',
  '的','了','吗','呢','吧','着','过','地','得','着',
  '儿','二','耳','而','发','法','飞','分','风','服',
  '高','哥','给','跟','工','公','共','古','果','过',
  '好','和','河','黑','很','后','花','话','回','或',
];

function mulberry32(a: number) {
  return function () {
    a |= 0; a = a + 0x6D2B79F5 | 0;
    let t = Math.imul(a ^ a >>> 15, 1 | a);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}

function pickChars(pool: string[], n: number, seed: number): string[] {
  const rng = mulberry32(seed);
  const arr = [...new Set(pool)];
  // Fisher-Yates with seeded RNG
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(rng() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr.slice(0, n);
}

function main() {
  const chars = pickChars(POOL, 20, SEED);
  const charset = {
    seed: SEED,
    generated_at: new Date().toISOString(),
    chars: chars.map((char, i) => ({ idx: i + 1, char })),
  };
  const here = dirname(fileURLToPath(import.meta.url));
  const out = join(here, '..', 'charset.json');
  mkdirSync(dirname(out), { recursive: true });
  writeFileSync(out, JSON.stringify(charset, null, 2) + '\n', 'utf8');
  console.log(`Wrote ${out} with ${chars.length} chars: ${chars.join(' ')}`);
}

main();
```

- [ ] **Step 2: 运行脚本生成 charset.json**

Run: `npx tsx poc/audio-tts/scripts/pick-charset.ts`
Expected: 输出 `Wrote .../charset.json with 20 chars: ...`，且 `poc/audio-tts/charset.json` 存在。

- [ ] **Step 3: 验证 charset.json 内容**

Run: `cat poc/audio-tts/charset.json | head -30`
Expected: 包含 `"seed": 42`、`"chars"` 数组长度 20、每项有 `idx` 和 `char` 两个字段。

- [ ] **Step 4: 提交**

```bash
git add poc/audio-tts/scripts/pick-charset.ts poc/audio-tts/charset.json
git commit -m "feat(poc): 抽取 20 字字表 (seed=42)"
```

---

## Task 3: Edge TTS baseline 生成器 `gen-edge.ts`

**Files:**
- Create: `poc/audio-tts/scripts/gen-edge.ts`
- Output: `poc/audio-tts/audio/edge/<idx>-<char>.mp3` × 20

**注意：** 不 import 主项目 `EdgeTtsService`，独立调用 msedge-tts，避免耦合。语音用 `zh-CN-XiaoxiaoNeural`（与主项目默认一致），输出格式 `AUDIO_24KHZ_48KBITRATE_MONO_MP3`，与 spec §5 对齐。

- [ ] **Step 1: 写脚本**

文件 `poc/audio-tts/scripts/gen-edge.ts`：

```typescript
import { readFileSync, writeFileSync, mkdirSync, existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const VOICE = 'zh-CN-XiaoxiaoNeural';

interface CharsetEntry { idx: number; char: string; }
interface Charset { seed: number; generated_at: string; chars: CharsetEntry[]; }

async function synth(text: string): Promise<Buffer> {
  const { MsEdgeTTS, OUTPUT_FORMAT } = await import('msedge-tts');
  const tts = new MsEdgeTTS();
  await tts.setMetadata(VOICE, OUTPUT_FORMAT.AUDIO_24KHZ_48KBITRATE_MONO_MP3);
  const { audioStream } = await tts.toStream(text);
  return await new Promise<Buffer>((resolve, reject) => {
    const chunks: Buffer[] = [];
    audioStream.on('data', (c: Buffer) => chunks.push(c));
    audioStream.on('end', () => resolve(Buffer.concat(chunks)));
    audioStream.on('error', reject);
  });
}

function pad2(n: number): string { return n.toString().padStart(2, '0'); }

async function main() {
  const here = dirname(fileURLToPath(import.meta.url));
  const charsetPath = join(here, '..', 'charset.json');
  const outDir = join(here, '..', 'audio', 'edge');
  mkdirSync(outDir, { recursive: true });
  const charset: Charset = JSON.parse(readFileSync(charsetPath, 'utf8'));

  for (const { idx, char } of charset.chars) {
    const fname = `${pad2(idx)}-${char}.mp3`;
    const out = join(outDir, fname);
    if (existsSync(out)) {
      console.log(`SKIP ${fname} (exists)`);
      continue;
    }
    console.log(`GEN  ${fname}`);
    const buf = await synth(char);
    if (!buf || buf.length === 0) throw new Error(`empty audio for ${char}`);
    writeFileSync(out, buf);
  }
  console.log(`Done. ${charset.chars.length} files in ${outDir}`);
}

main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: 运行脚本**

Run: `npx tsx poc/audio-tts/scripts/gen-edge.ts`
Expected: 打印 20 行 `GEN  01-妈.mp3` 等，最后 `Done. 20 files in ...`。

- [ ] **Step 3: 抽检文件**

Run: `ls poc/audio-tts/audio/edge | wc -l && file poc/audio-tts/audio/edge/01-*.mp3`
Expected: 数量 20；`file` 输出包含 `MPEG ADTS, layer III` 或 `Audio file`。

- [ ] **Step 4: 提交脚本（不提交 mp3）**

```bash
git add poc/audio-tts/scripts/gen-edge.ts
git commit -m "feat(poc): Edge TTS baseline 生成器"
```

---

## Task 4: GPT-SoVITS 生成器 `gen-sovits.py`

**Files:**
- Create: `poc/audio-tts/scripts/requirements-sovits.txt`
- Create: `poc/audio-tts/scripts/gen-sovits.py`
- Output: `poc/audio-tts/audio/sovits/<idx>-<char>.mp3` × 20

**说明：** GPT-SoVITS 上游仓库（`RVC-Boss/GPT-SoVITS`）以 git clone 形式分发，本地推理需 clone 并加载 release demo 权重。这里采用社区维护的 PyPI 包 `gpt-sovits-tts` 作为简单封装；如果失效则在 README 中记录改用源码 clone 方案（Task 4 内 Step 1 中的 fallback 注释）。输出 wav 后用 ffmpeg 转 mp3 24kHz 48k mono，对齐 edge 输出。

- [ ] **Step 1: 写依赖文件**

文件 `poc/audio-tts/scripts/requirements-sovits.txt`：

```
# GPT-SoVITS 推理封装。
# 若该包安装失败，README 中记录改用 git clone https://github.com/RVC-Boss/GPT-SoVITS 的源码方式，
# 并将下面 generate() 的实现替换为对源码 inference_webui 的本地调用。
gpt-sovits-tts>=0.4
torch>=2.0
torchaudio>=2.0
soundfile>=0.12
```

- [ ] **Step 2: 写生成脚本**

文件 `poc/audio-tts/scripts/gen-sovits.py`：

```python
"""
读 ../charset.json，对每个汉字用 GPT-SoVITS 默认中文 demo 模型合成，
输出到 ../audio/sovits/<idx>-<char>.mp3。
"""
from __future__ import annotations
import json
import os
import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

HERE = Path(__file__).resolve().parent
CHARSET = HERE.parent / "charset.json"
OUT_DIR = HERE.parent / "audio" / "sovits"


def ensure_ffmpeg() -> None:
    if shutil.which("ffmpeg") is None:
        sys.exit("ffmpeg not found; install via `brew install ffmpeg`")


def synth_wav(text: str, wav_out: Path) -> None:
    """调用 GPT-SoVITS 默认中文音色合成单字到 wav。

    若 gpt-sovits-tts 包接口在使用时变化，按其 README 调整 import 与调用方式。
    """
    from gpt_sovits_tts import GPTSoVITS  # type: ignore

    tts = GPTSoVITS(language="zh")  # 使用包内默认 demo 音色
    tts.synthesize(text=text, output_path=str(wav_out))


def to_mp3(wav: Path, mp3: Path) -> None:
    subprocess.run(
        [
            "ffmpeg", "-y", "-loglevel", "error",
            "-i", str(wav),
            "-ar", "24000", "-ac", "1", "-b:a", "48k",
            str(mp3),
        ],
        check=True,
    )


def main() -> None:
    ensure_ffmpeg()
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    charset = json.loads(CHARSET.read_text(encoding="utf-8"))
    for entry in charset["chars"]:
        idx = entry["idx"]
        char = entry["char"]
        fname = f"{idx:02d}-{char}.mp3"
        mp3 = OUT_DIR / fname
        if mp3.exists():
            print(f"SKIP {fname} (exists)")
            continue
        print(f"GEN  {fname}")
        with tempfile.TemporaryDirectory() as td:
            wav = Path(td) / "out.wav"
            synth_wav(char, wav)
            to_mp3(wav, mp3)
    print(f"Done. {len(charset['chars'])} files in {OUT_DIR}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 创建 venv 并安装**

Run:
```bash
python3 -m venv poc/audio-tts/.venv-sovits
source poc/audio-tts/.venv-sovits/bin/activate
pip install -r poc/audio-tts/scripts/requirements-sovits.txt
```
Expected: 安装成功无 error；如失败按 `requirements-sovits.txt` 注释切换源码方案，并把改动写进 README。

- [ ] **Step 4: 运行脚本**

Run: `python poc/audio-tts/scripts/gen-sovits.py`
Expected: 20 个 mp3 落到 `poc/audio-tts/audio/sovits/`。首跑会下载模型（数百 MB），耐心等待。

- [ ] **Step 5: 抽检**

Run: `deactivate; ls poc/audio-tts/audio/sovits | wc -l`
Expected: 20。

- [ ] **Step 6: 提交脚本**

```bash
git add poc/audio-tts/scripts/gen-sovits.py poc/audio-tts/scripts/requirements-sovits.txt
git commit -m "feat(poc): GPT-SoVITS 生成器"
```

---

## Task 5: CoquiTTS 生成器 `gen-coqui.py`

**Files:**
- Create: `poc/audio-tts/scripts/requirements-coqui.txt`
- Create: `poc/audio-tts/scripts/gen-coqui.py`
- Output: `poc/audio-tts/audio/coqui/<idx>-<char>.mp3` × 20

- [ ] **Step 1: 写依赖文件**

文件 `poc/audio-tts/scripts/requirements-coqui.txt`：

```
# Coqui TTS（社区维护 fork，原仓库已归档）
coqui-tts>=0.24
torch>=2.0
soundfile>=0.12
```

- [ ] **Step 2: 写生成脚本**

文件 `poc/audio-tts/scripts/gen-coqui.py`：

```python
"""
读 ../charset.json，用 CoquiTTS `tts_models/zh-CN/baker/tacotron2-DDC-GST` 合成，
输出 ../audio/coqui/<idx>-<char>.mp3。
"""
from __future__ import annotations
import json
import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

HERE = Path(__file__).resolve().parent
CHARSET = HERE.parent / "charset.json"
OUT_DIR = HERE.parent / "audio" / "coqui"
MODEL_NAME = "tts_models/zh-CN/baker/tacotron2-DDC-GST"


def ensure_ffmpeg() -> None:
    if shutil.which("ffmpeg") is None:
        sys.exit("ffmpeg not found; install via `brew install ffmpeg`")


def make_tts():
    from TTS.api import TTS  # type: ignore
    return TTS(model_name=MODEL_NAME, progress_bar=False)


def synth_wav(tts, text: str, wav_out: Path) -> None:
    tts.tts_to_file(text=text, file_path=str(wav_out))


def to_mp3(wav: Path, mp3: Path) -> None:
    subprocess.run(
        [
            "ffmpeg", "-y", "-loglevel", "error",
            "-i", str(wav),
            "-ar", "24000", "-ac", "1", "-b:a", "48k",
            str(mp3),
        ],
        check=True,
    )


def main() -> None:
    ensure_ffmpeg()
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    charset = json.loads(CHARSET.read_text(encoding="utf-8"))
    tts = make_tts()
    for entry in charset["chars"]:
        idx = entry["idx"]
        char = entry["char"]
        fname = f"{idx:02d}-{char}.mp3"
        mp3 = OUT_DIR / fname
        if mp3.exists():
            print(f"SKIP {fname} (exists)")
            continue
        print(f"GEN  {fname}")
        with tempfile.TemporaryDirectory() as td:
            wav = Path(td) / "out.wav"
            synth_wav(tts, char, wav)
            to_mp3(wav, mp3)
    print(f"Done. {len(charset['chars'])} files in {OUT_DIR}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 创建 venv 并安装**

Run:
```bash
python3 -m venv poc/audio-tts/.venv-coqui
source poc/audio-tts/.venv-coqui/bin/activate
pip install -r poc/audio-tts/scripts/requirements-coqui.txt
```
Expected: 安装成功。

- [ ] **Step 4: 运行脚本**

Run: `python poc/audio-tts/scripts/gen-coqui.py`
Expected: 首跑下载模型；最终 20 个 mp3 落到 `poc/audio-tts/audio/coqui/`。

- [ ] **Step 5: 抽检并退出 venv**

Run: `deactivate; ls poc/audio-tts/audio/coqui | wc -l`
Expected: 20。

- [ ] **Step 6: 提交脚本**

```bash
git add poc/audio-tts/scripts/gen-coqui.py poc/audio-tts/scripts/requirements-coqui.txt
git commit -m "feat(poc): CoquiTTS 生成器"
```

---

## Task 6: 打分对比页面 `compare.html`

**Files:**
- Create: `poc/audio-tts/scripts/build-compare-page.ts`
- Output: `poc/audio-tts/compare.html`（gitignore，本地生成）

**功能：**

- 读 `charset.json`，把字表和"哪几家有 mp3"探测结果 inline 到 HTML 里。
- 行=字，列=A/B/C（盲听模式默认开）；每行的引擎顺序按 `idx` 单独打乱。
- 1-5 星点击或键盘 1-5 打分；进度写 localStorage。
- 顶部按钮：导出 CSV、清空打分、切换盲听。
- 列名在盲听模式下是 A/B/C；导出 CSV 时还原真名。
- 引擎缺失（对应目录不存在）的列显示"未生成，跳过"。

- [ ] **Step 1: 写脚本（生成 HTML）**

文件 `poc/audio-tts/scripts/build-compare-page.ts`：

```typescript
import { readFileSync, writeFileSync, existsSync, readdirSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

interface CharsetEntry { idx: number; char: string; }
interface Charset { seed: number; generated_at: string; chars: CharsetEntry[]; }

const ENGINES = ['edge', 'sovits', 'coqui'] as const;
type Engine = typeof ENGINES[number];

function pad2(n: number): string { return n.toString().padStart(2, '0'); }

function detectAvailable(audioRoot: string): Record<Engine, boolean> {
  const out: Record<Engine, boolean> = { edge: false, sovits: false, coqui: false };
  for (const e of ENGINES) {
    const dir = join(audioRoot, e);
    out[e] = existsSync(dir) && readdirSync(dir).some((f) => f.endsWith('.mp3'));
  }
  return out;
}

function main() {
  const here = dirname(fileURLToPath(import.meta.url));
  const charset: Charset = JSON.parse(readFileSync(join(here, '..', 'charset.json'), 'utf8'));
  const audioRoot = join(here, '..', 'audio');
  const available = detectAvailable(audioRoot);

  const rows = charset.chars.map(({ idx, char }) => ({
    idx, char,
    files: Object.fromEntries(
      ENGINES.map((e) => [e, available[e] ? `audio/${e}/${pad2(idx)}-${char}.mp3` : null])
    ) as Record<Engine, string | null>,
  }));

  const html = renderHtml(rows, available);
  const out = join(here, '..', 'compare.html');
  writeFileSync(out, html, 'utf8');
  console.log(`Wrote ${out}. Available: ${ENGINES.filter((e) => available[e]).join(', ') || 'NONE'}`);
}

function renderHtml(
  rows: { idx: number; char: string; files: Record<Engine, string | null> }[],
  available: Record<Engine, boolean>,
): string {
  const data = JSON.stringify({ rows, available, engines: ENGINES });
  return `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8" />
<title>字音 TTS 横评</title>
<style>
  body { font-family: system-ui, sans-serif; max-width: 1000px; margin: 20px auto; padding: 0 16px; }
  table { width: 100%; border-collapse: collapse; }
  th, td { border: 1px solid #ddd; padding: 8px; text-align: center; vertical-align: middle; }
  th { background: #f5f5f5; }
  .star { cursor: pointer; font-size: 22px; color: #ccc; user-select: none; padding: 0 2px; }
  .star.on { color: #f5b301; }
  .char { font-size: 28px; font-weight: bold; }
  .toolbar { margin: 12px 0; display: flex; gap: 12px; align-items: center; flex-wrap: wrap; }
  button { padding: 6px 12px; cursor: pointer; }
  .skip { color: #999; font-style: italic; }
  .summary { margin-top: 20px; padding: 12px; background: #f5f5f5; border-radius: 4px; }
  audio { width: 180px; vertical-align: middle; }
</style>
</head>
<body>
<h1>字音 TTS 横评</h1>
<div class="toolbar">
  <label><input type="checkbox" id="blind" checked /> 盲听模式（按字打乱列顺序）</label>
  <button id="export">导出 CSV</button>
  <button id="clear">清空打分</button>
  <span id="status"></span>
</div>
<table>
  <thead>
    <tr>
      <th>序号</th><th>字</th>
      <th data-col="0">A</th>
      <th data-col="1">B</th>
      <th data-col="2">C</th>
    </tr>
  </thead>
  <tbody id="tbody"></tbody>
</table>
<div class="summary" id="summary"></div>

<script>
const DATA = ${data};
const STORAGE_KEY = 'tts-poc-scores-v1';

function mulberry32(a) {
  return function () {
    a |= 0; a = a + 0x6D2B79F5 | 0;
    let t = Math.imul(a ^ a >>> 15, 1 | a);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}

function shuffleSeeded(arr, seed) {
  const rng = mulberry32(seed);
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(rng() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

function loadScores() {
  try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}'); }
  catch { return {}; }
}
function saveScores(s) { localStorage.setItem(STORAGE_KEY, JSON.stringify(s)); }

let scores = loadScores();
const blindCheckbox = document.getElementById('blind');

function colOrderFor(idx) {
  // blind: 按字 idx 打乱；non-blind: edge,sovits,coqui 固定
  if (blindCheckbox.checked) return shuffleSeeded(DATA.engines, idx);
  return [...DATA.engines];
}

function render() {
  const tbody = document.getElementById('tbody');
  tbody.innerHTML = '';
  for (const row of DATA.rows) {
    const order = colOrderFor(row.idx);
    const tr = document.createElement('tr');
    tr.innerHTML = '<td>' + String(row.idx).padStart(2, '0') + '</td>'
      + '<td class="char">' + row.char + '</td>';
    for (const eng of order) {
      const td = document.createElement('td');
      const file = row.files[eng];
      if (!file) {
        td.innerHTML = '<span class="skip">未生成</span>';
      } else {
        const audio = '<audio controls preload="none" src="' + file + '"></audio>';
        const stars = [1,2,3,4,5].map((n) => {
          const cur = (scores[row.idx] || {})[eng] || 0;
          return '<span class="star' + (n <= cur ? ' on' : '') + '" data-idx="' + row.idx + '" data-eng="' + eng + '" data-n="' + n + '">★</span>';
        }).join('');
        td.innerHTML = audio + '<br/>' + stars;
      }
      tr.appendChild(td);
    }
    tbody.appendChild(tr);
  }
  bindStars();
  renderSummary();
}

function bindStars() {
  document.querySelectorAll('.star').forEach((el) => {
    el.addEventListener('click', () => {
      const idx = el.getAttribute('data-idx');
      const eng = el.getAttribute('data-eng');
      const n = Number(el.getAttribute('data-n'));
      scores[idx] = scores[idx] || {};
      scores[idx][eng] = scores[idx][eng] === n ? 0 : n;
      saveScores(scores);
      render();
    });
  });
}

function renderSummary() {
  const totals = {}; const counts = {};
  for (const e of DATA.engines) { totals[e] = 0; counts[e] = 0; }
  for (const row of DATA.rows) {
    const r = scores[row.idx] || {};
    for (const e of DATA.engines) {
      if (r[e] && row.files[e]) { totals[e] += r[e]; counts[e]++; }
    }
  }
  const parts = DATA.engines.map((e) => {
    if (!DATA.available[e]) return e + ': N/A';
    const avg = counts[e] ? (totals[e] / counts[e]).toFixed(2) : '-';
    return e + ' 平均 ' + avg + ' (' + counts[e] + '/' + DATA.rows.length + ')';
  });
  document.getElementById('summary').textContent = '汇总：' + parts.join(' | ');
}

document.getElementById('export').addEventListener('click', () => {
  const lines = ['idx,char,edge_score,sovits_score,coqui_score,notes'];
  for (const row of DATA.rows) {
    const r = scores[row.idx] || {};
    lines.push([
      row.idx, row.char,
      r.edge || '', r.sovits || '', r.coqui || '', '',
    ].join(','));
  }
  const blob = new Blob([lines.join('\\n')], { type: 'text/csv' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url; a.download = 'scores-' + Date.now() + '.csv';
  a.click(); URL.revokeObjectURL(url);
});

document.getElementById('clear').addEventListener('click', () => {
  if (!confirm('清空所有打分？')) return;
  scores = {}; saveScores(scores); render();
});

blindCheckbox.addEventListener('change', render);

render();
</script>
</body>
</html>
`;
}

main();
```

- [ ] **Step 2: 运行脚本生成 HTML**

Run: `npx tsx poc/audio-tts/scripts/build-compare-page.ts`
Expected: 输出 `Wrote .../compare.html. Available: edge, sovits, coqui`（取决于哪些目录有 mp3）。

- [ ] **Step 3: 浏览器打开校验**

Run: `open poc/audio-tts/compare.html`
Expected: 浏览器显示 20 行字，每行 A/B/C 三列各有 audio 控件 + 5 颗星；点 ★ 立即变金；刷新页面打分还在；切换"盲听模式"列顺序变化。点击"导出 CSV"下载到一个 `scores-*.csv`，列头为 `idx,char,edge_score,sovits_score,coqui_score,notes`。

- [ ] **Step 4: 提交脚本（compare.html 已 gitignore）**

```bash
git add poc/audio-tts/scripts/build-compare-page.ts
git commit -m "feat(poc): 打分对比页面生成器"
```

---

## Task 7: 端到端干跑 + 收尾

**目的：** 验证整个流水线能从 0 跑通，把 README 里写的命令逐条执行一次；产出第一份 CSV 用于决策。

- [ ] **Step 1: 清空缓存，从头跑**

Run:
```bash
rm -rf poc/audio-tts/audio poc/audio-tts/compare.html
npx tsx poc/audio-tts/scripts/pick-charset.ts
npx tsx poc/audio-tts/scripts/gen-edge.ts
```
Expected: charset.json 与 Task 2 输出一致（seed=42 确定性）；`audio/edge/` 下 20 个 mp3。

- [ ] **Step 2: 跑 sovits 与 coqui（按 README 指令）**

按 README "跑通流程" 中 Python 部分依次执行。任一引擎装不上记录在 README 末尾"已知问题"小节，但**不阻塞**后续步骤（打分页会在该列显示"未生成"）。

- [ ] **Step 3: 生成对比页**

Run: `npx tsx poc/audio-tts/scripts/build-compare-page.ts && open poc/audio-tts/compare.html`
Expected: 页面打开后 ▶️ 能播；可生成的引擎正常显示 5 星；缺失的列显示"未生成"。

- [ ] **Step 4: 完成首轮打分并导出 CSV**

人工：戴耳机，打完 20 行 × 已生成引擎数。点"导出 CSV"，把下载的文件移到 `poc/audio-tts/results/`。

Run: `mv ~/Downloads/scores-*.csv poc/audio-tts/results/`
Expected: `ls poc/audio-tts/results/*.csv` 至少 1 个文件。

- [ ] **Step 5: 在 README 末尾追加结论**

文件 `poc/audio-tts/README.md` 末尾追加：

```markdown
## 首轮结果（YYYY-MM-DD）

- 平均分：edge=X.X / sovits=X.X / coqui=X.X
- CSV：`results/scores-XXXXXXXXXXXXX.csv`
- 决策：按 README "结论判读规则" → [继续做正式预生成 / 转云 TTS / 维持现状]
- 后续：[新 design 链接 或 关闭 POC]
```

把占位符填上真实数字与判定。

- [ ] **Step 6: 提交结论**

```bash
git add poc/audio-tts/README.md poc/audio-tts/results/scores-*.csv
git commit -m "docs(poc): 字音 TTS 横评首轮结果与决策"
```

---

## Self-Review

**Spec coverage:**

| Spec 节 | 对应 Task |
|---|---|
| §1 目标 / 非目标 | Task 1（README 写明）、所有 task 不动 server/client |
| §2 决策记录 | Task 1 README + spec 链接 |
| §3 字表 | Task 2 |
| §4 目录结构 | Task 1（骨架）+ Task 2-6（落实） |
| §5 引擎与模型 | Task 3 / 4 / 5 |
| §6 打分对比页面 | Task 6 |
| §7 结论判读规则 | Task 1 README + Task 7 Step 5 |
| §8 范围/风险/兜底 | Task 1 README + Task 4 fallback 注释 + Task 7 Step 2 允许跳过 |
| §9 后续 | Task 7 Step 5 |

**Placeholder scan:** 仅 Task 7 Step 5 的 README 模板有故意留白（运行后填实数），其余无 TBD/TODO/"add appropriate X"。

**Type/命名一致性：**
- charset 结构 `{ seed, generated_at, chars: [{idx, char}] }` 在 Task 2/3/4/5/6 全部一致。
- mp3 文件名格式 `<两位序号>-<字>.mp3` 在 Task 3/4/5/6 一致。
- 引擎键名 `edge` / `sovits` / `coqui` 在所有脚本与 CSV 列头一致。
- 输出格式 mp3 24kHz 单声道 48kbps 在 Task 3 / 4 / 5 一致。
